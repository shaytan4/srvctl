os:
  reboot:
    - "reboot"
  update_os:
    - "apt-get update -y"
  date:
    - "date"
  explorer:
    - "explorer.exe"

plex:
  update:
    - "plex update command"
  restart:
    - "plex server restart"